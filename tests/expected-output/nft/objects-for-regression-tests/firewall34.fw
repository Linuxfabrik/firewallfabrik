#!/bin/sh
#
#  This file is managed by FirewallFabrik - do not edit
#
#  FirewallFabrik fwf-nft VERSION
#
#  Generated TIMESTAMP
#
# files: * firewall34.fw fwf.sh
#
#  testing AddressTable object
#
# Rule 0 (NAT): Cannot resolve address for object type AddressTable
# Rule 0 (NAT): Interface "eth0.100" has no addresses
# Rule 1 (NAT): Cannot resolve address for object type AddressTable
# Rule 1 (NAT): Interface "eth0.100" has no addresses
# AddressTable "addr-table-1": file not found (addr-table-1.tbl)
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Cannot resolve address for object type AddressTable
# AddressTable "empty table": file not found (emtpy-table.tbl)
# AddressTable "addr-table-1": file not found (addr-table-1.tbl)
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 0 (NAT): Cannot resolve address for object type AddressTable
# Rule 0 (NAT): Interface "eth0.100" has no addresses
# AddressTable "addr-table-1": file not found (addr-table-1.tbl)
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
# Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Could not resolve any destination addresses
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Could not resolve any destination addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
# Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
# Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Could not resolve any destination addresses
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Could not resolve any destination addresses
# AddressTable "empty table": file not found (emtpy-table.tbl)
# AddressTable "addr-table-1": file not found (addr-table-1.tbl)
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
# AddressTable "addr-table-1": file not found (addr-table-1.tbl)

FWBDEBUG=""

PATH="/sbin:/usr/sbin:/bin:/usr/bin:${PATH}"
export PATH

NFT="/usr/sbin/nft"
IP="ip"
LOGGER="logger"

log() {
    echo "$1"
    command -v "$LOGGER" >/dev/null 2>&1 && $LOGGER -p info "$1"
}

getInterfaceVarName() {
    echo "$1" | sed 's/\./_/'
}

getaddr_internal() {
    dev=$1
    name=$2
    af=$3
    L=$("$IP" "$af" addr show dev "$dev" | sed -n '/inet/{s!.*inet6* !!;s!/.*!!p}' | sed 's/peer.*//')
    test -z "$L" && {
        eval "$name=''"
        return
    }
    eval "${name}_list=\"$L\""
}

getnet_internal() {
    dev=$1
    name=$2
    af=$3
    L=$("$IP" route list proto kernel | grep "$dev" | grep -v default | sed 's! .*$!!')
    test -z "$L" && {
        eval "$name=''"
        return
    }
    eval "${name}_list=\"$L\""
}

getaddr() {
    getaddr_internal "$1" "$2" "-4"
}

getaddr6() {
    getaddr_internal "$1" "$2" "-6"
}

getnet() {
    getnet_internal "$1" "$2" "-4"
}

getnet6() {
    getnet_internal "$1" "$2" "-6"
}

# function getinterfaces is used to process wildcard interfaces
getinterfaces() {
    NAME=$1
    "$IP" link show | grep ": $NAME" | while read -r L; do
        OIFS=$IFS
        IFS=" :"
        # shellcheck disable=SC2086
        set -- $L
        IFS=$OIFS
        echo "$2"
    done
}

diff_intf() {
    func=$1
    list1=$2
    list2=$3
    cmd=$4
    for intf in $list1
    do
        echo "$list2" | grep -q "$intf" || {
        # $intf is absent in list 2
            "$func" "$intf" "$cmd"
        }
    done
}

P2P_INTERFACE_WARNING=""

missing_address() {
    address=$1
    cmd=$2

    oldIFS=$IFS
    IFS="@"
    # shellcheck disable=SC2086
    set -- $address
    addr=$1
    interface=$2
    IFS=$oldIFS

    "$IP" addr show dev "$interface" | grep -q POINTOPOINT && {
        test -z "$P2P_INTERFACE_WARNING" && echo "Warning: Can not update address of interface $interface. fwbuilder can not manage addresses of point-to-point interfaces yet"
        P2P_INTERFACE_WARNING="yes"
        return
    }

    test "$cmd" = "add" && {
      echo "# Adding ip address: $interface $addr"
      if echo "$addr" | grep -q ':'; then
          $FWBDEBUG "$IP" addr "$cmd" "$addr" dev "$interface"
      else
          $FWBDEBUG "$IP" addr "$cmd" "$addr" broadcast + dev "$interface"
      fi
    }

    test "$cmd" = "del" && {
      echo "# Removing ip address: $interface $addr"
      $FWBDEBUG "$IP" addr "$cmd" "$addr" dev "$interface" || exit 1
    }

    $FWBDEBUG "$IP" link set "$interface" up
}

list_addresses_by_scope() {
    interface=$1
    scope=$2
    ignore_list=$3
    "$IP" addr ls dev "$interface" | \
      awk -v IGNORED="$ignore_list" -v SCOPE="$scope" \
        'BEGIN {
           split(IGNORED,ignored_arr);
           for (a in ignored_arr) {ignored_dict[ignored_arr[a]]=1;}
         }
         (/inet |inet6 / && $0 ~ SCOPE && !($2 in ignored_dict)) {print $2;}' | \
        while read -r addr; do
          echo "${addr}@$interface"
	done | sort
}

update_addresses_of_interface() {
    ignore_list=$2
    # shellcheck disable=SC2086
    set -- $1
    interface=$1
    shift

    FWB_ADDRS=$(
      for addr in "$@"; do
        echo "${addr}@$interface"
      done | sort
    )

    CURRENT_ADDRS_ALL_SCOPES=""
    CURRENT_ADDRS_GLOBAL_SCOPE=""

    if "$IP" link show dev "$interface" >/dev/null 2>&1; then
      CURRENT_ADDRS_ALL_SCOPES=$(list_addresses_by_scope "$interface" 'scope .*' "$ignore_list")
      CURRENT_ADDRS_GLOBAL_SCOPE=$(list_addresses_by_scope "$interface" 'scope global' "$ignore_list")
    else
      echo "# Interface $interface does not exist"
      # Stop the script if we are not in test mode
      test -z "$FWBDEBUG" && exit 1
    fi

    diff_intf missing_address "$FWB_ADDRS" "$CURRENT_ADDRS_ALL_SCOPES" add
    diff_intf missing_address "$CURRENT_ADDRS_GLOBAL_SCOPE" "$FWB_ADDRS" del
}

clear_addresses_except_known_interfaces() {
    "$IP" link show | sed 's/://g' | awk -v IGNORED="$*" \
        'BEGIN {
           split(IGNORED,ignored_arr);
           for (a in ignored_arr) {ignored_dict[ignored_arr[a]]=1;}
         }
         (/state/ && !($2 in ignored_dict)) {print $2;}' | \
         while read -r intf; do
            echo "# Removing addresses not configured in fwbuilder from interface $intf"
            $FWBDEBUG "$IP" addr flush dev "$intf" scope global
            $FWBDEBUG "$IP" link set "$intf" down
         done
}

check_nft() {
    if [ ! -x "$NFT" ]; then
        echo "Error: nft not found at $NFT"
        exit 1
    fi
}

prolog_commands() {
    echo "Running prolog script"

    echo "Prolog script done"
}

epilog_commands() {
    echo "Running epilog script"

    echo "Epilog script done"
}

run_epilog_and_exit() {
    epilog_commands
    exit "$1"
}

configure_interfaces() {
    :
    getaddr eth0.100  i_eth0_100
    getaddr6 eth0.100  i_eth0_100_v6
    getnet eth0.100  i_eth0_100_network
    getnet6 eth0.100  i_eth0_100_v6_network
    update_addresses_of_interface "eth1 192.168.1.100/24" ""
    update_addresses_of_interface "lo 127.0.0.1/8" ""
}

script_body() {
    $NFT -f /dev/stdin <<'NFT_RULES'
table inet fwf_filter {}
delete table inet fwf_filter
table ip fwf_nat {}
delete table ip fwf_nat

table inet fwf_filter {
    chain input {
        type filter hook input priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 5 (global)
        #
        # # Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
        log prefix "RULE 5 -- DENY on global " level debug drop
        #
        # Rule 6 (global)
        #
        # # Rule 6 (global): Cannot resolve address for object type AddressTable
        ip saddr 61.150.47.112 log prefix "RULE 6 -- DENY on global " level debug drop
        #
        # Rule 7 (global)
        #
        # # Rule 7 (global): Cannot resolve address for object type AddressTable
        ip saddr != 61.150.47.112 ct state new accept
        #
        # Rule 10 (global)
        #
        ip saddr 192.168.1.0/24 ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule 14 (global)
        #
        # using connlimit
        # option. Connlimit
        # is only valid in combination
        # with "-p tcp -m tcp"
        ip saddr 192.168.1.0/24 tcp dport 25 ct state new accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 5 (global)
        #
        # # Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
        log prefix "RULE 5 -- DENY on global " level debug drop
        #
        # Rule 6 (global)
        #
        # # Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
        log prefix "RULE 6 -- DENY on global " level debug drop
        #
        # Rule 7 (global)
        #
        # # Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
        ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule Policy_ipv6 0 (global)
        #
        ct state new accept
        #
        # Rule Policy_ipv6 1 (global)
        #
        log prefix "RULE 1 -- DENY on global " level debug drop
    }

    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 1 (global)
        #
        # # Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
        log prefix "RULE 1 -- DENY on global " level debug drop
        #
        # Rule 2 (global)
        #
        ip daddr 61.150.47.112 log prefix "RULE 2 -- DENY on global " level debug drop
        #
        # Rule 3 (global)
        #
        # # Rule 3 (global): Cannot resolve address for object type AddressTable
        ip daddr 61.150.47.112 tcp dport 25 drop
        #
        # Rule 5 (global)
        #
        # # Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
        log prefix "RULE 5 -- DENY on global " level debug drop
        #
        # Rule 6 (global)
        #
        # # Rule 6 (global): Cannot resolve address for object type AddressTable
        ip saddr 61.150.47.112 log prefix "RULE 6 -- DENY on global " level debug drop
        #
        # Rule 7 (global)
        #
        # # Rule 7 (global): Cannot resolve address for object type AddressTable
        ip saddr != 61.150.47.112 ct state new accept
        #
        # Rule 9 (global)
        #
        ip daddr 192.168.1.10 tcp dport 25 ct state new accept
        #
        # Rule 10 (global)
        #
        ip saddr 192.168.1.0/24 ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule 13 (global)
        #
        # using address table
        # object with no addresses
        ip daddr 22.22.22.0/24 log prefix "RULE 13 -- DENY on global " level debug drop
        #
        # Rule 14 (global)
        #
        # using connlimit
        # option. Connlimit
        # is only valid in combination
        # with "-p tcp -m tcp"
        ip saddr 192.168.1.0/24 tcp dport 25 ct state new accept
        #
        # Rule 15 (global)
        #
        # # Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
        ct state new accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 1 (global)
        #
        # # Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
        log prefix "RULE 1 -- DENY on global " level debug drop
        #
        # Rule 3 (global)
        #
        # # Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
        tcp dport 25 drop
        #
        # Rule 5 (global)
        #
        # # Rule 5 (global): Cannot resolve address for object type AddressTable
# Rule 5 (global): Could not resolve any source addresses
        log prefix "RULE 5 -- DENY on global " level debug drop
        #
        # Rule 6 (global)
        #
        # # Rule 6 (global): Cannot resolve address for object type AddressTable
# Rule 6 (global): Could not resolve any source addresses
        log prefix "RULE 6 -- DENY on global " level debug drop
        #
        # Rule 7 (global)
        #
        # # Rule 7 (global): Cannot resolve address for object type AddressTable
# Rule 7 (global): Could not resolve any source addresses
        ct state new accept
        #
        # Rule 9 (global)
        #
        tcp dport 25 ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule 15 (global)
        #
        # # Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
        ct state new accept
        #
        # Rule Policy_ipv6 0 (global)
        #
        ct state new accept
        #
        # Rule Policy_ipv6 1 (global)
        #
        log prefix "RULE 1 -- DENY on global " level debug drop
    }

    chain output {
        type filter hook output priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 1 (global)
        #
        # # Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
        log prefix "RULE 1 -- DENY on global " level debug drop
        #
        # Rule 2 (global)
        #
        ip daddr 61.150.47.112 log prefix "RULE 2 -- DENY on global " level debug drop
        #
        # Rule 3 (global)
        #
        # # Rule 3 (global): Cannot resolve address for object type AddressTable
        ip daddr 61.150.47.112 tcp dport 25 drop
        #
        # Rule 4 (global)
        #
        # # Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
        ip daddr 61.150.47.112 tcp dport 25 drop
        #
        # Rule 8 (global)
        #
        # # Rule 8 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
        ip daddr != 61.150.47.112 ct state new accept
        #
        # Rule 9 (global)
        #
        ip daddr 192.168.1.10 tcp dport 25 ct state new accept
        #
        # Rule 10 (global)
        #
        ip saddr 192.168.1.0/24 ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule 13 (global)
        #
        # using address table
        # object with no addresses
        ip daddr 22.22.22.0/24 log prefix "RULE 13 -- DENY on global " level debug drop
        #
        # Rule 14 (global)
        #
        # using connlimit
        # option. Connlimit
        # is only valid in combination
        # with "-p tcp -m tcp"
        ip saddr 192.168.1.0/24 tcp dport 25 ct state new accept
        #
        # Rule 15 (global)
        #
        # # Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
        ct state new accept
        #
        # Rule 0 (global)
        #
        ct state new accept
        #
        # Rule 1 (global)
        #
        # # Rule 1 (global): Cannot resolve address for object type AddressTable
# Rule 1 (global): Could not resolve any destination addresses
        log prefix "RULE 1 -- DENY on global " level debug drop
        #
        # Rule 3 (global)
        #
        # # Rule 3 (global): Cannot resolve address for object type AddressTable
# Rule 3 (global): Could not resolve any destination addresses
        tcp dport 25 drop
        #
        # Rule 4 (global)
        #
        # # Rule 4 (global): Cannot resolve address for object type AddressTable
# Rule 4 (global): Could not resolve any destination addresses
# Rule 4 (global): Could not resolve any source addresses
# Rule 4 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
        tcp dport 25 drop
        #
        # Rule 8 (global)
        #
        # # Rule 8 (global): Cannot resolve address for object type AddressTable
# Rule 8 (global): Could not resolve any destination addresses
# Rule 8 (global): Could not resolve any source addresses
# Rule 8 (global): Dynamic interface address not yet supported by nftables compiler (interface: eth0.100)
        ct state new accept
        #
        # Rule 9 (global)
        #
        tcp dport 25 ct state new accept
        #
        # Rule 11 (global)
        #
        log prefix "RULE 11 -- DENY on global " level debug drop
        #
        # Rule 12 (global)
        #
        # using address table
        # object with no addresses
        log prefix "RULE 12 -- DENY on global " level debug drop
        #
        # Rule 15 (global)
        #
        # # Rule 15 (global): Cannot resolve address for object type AddressTable
# Rule 15 (global): Could not resolve any destination addresses
        ct state new accept
        #
        # Rule Policy_ipv6 0 (global)
        #
        ct state new accept
        #
        # Rule Policy_ipv6 1 (global)
        #
        log prefix "RULE 1 -- DENY on global " level debug drop
    }
}

table ip fwf_nat {
    chain prerouting {
        type nat hook prerouting priority dstnat;
        #
        # Rule 0 (NAT)
        #
        tcp dport 25 dnat to 192.168.1.10
        #
        # Rule 0 (NAT)
        #
        tcp dport 25 dnat to 192.168.1.10
    }

    chain postrouting {
        type nat hook postrouting priority srcnat;
        #
        # Rule 1 (NAT)
        #
        oifname "eth0.100" ip saddr 192.168.1.0/24 masquerade
    }
}

NFT_RULES
}

ip_forward() {
    :

}

flush_own_tables() {
    # remove only our own tables, leaving other tools' tables untouched
    $NFT delete table inet fwf_filter 2>/dev/null
    $NFT delete table ip fwf_filter 2>/dev/null
    $NFT delete table inet fwf_nat 2>/dev/null
    $NFT delete table ip fwf_nat 2>/dev/null
}

block_action() {
    $NFT flush ruleset 2>/dev/null
    # block all traffic by default (always use inet to block both IPv4 and IPv6)
    $NFT add table inet fwf_filter
    $NFT add chain inet fwf_filter input '{ type filter hook input priority 0; policy drop; }'
    $NFT add chain inet fwf_filter forward '{ type filter hook forward priority 0; policy drop; }'
    $NFT add chain inet fwf_filter output '{ type filter hook output priority 0; policy drop; }'
}

stop_action() {
    $NFT flush ruleset 2>/dev/null
}

check_nft_status() {
    tables=$($NFT list tables 2>/dev/null)
    if echo "$tables" | grep -q 'fwf_filter'; then
        return 0
    fi
    return 3
}

status_action() {
    check_nft_status
    ret=$?
    if [ $ret -eq 0 ]; then
        echo "Firewall is active"
        return 0
    fi
    echo "Firewall is not configured"
    exit 3
}

# See how we were called.
# For backwards compatibility missing argument is equivalent to 'start'

cmd=$1
test -z "$cmd" && {
    cmd="start"
}

case "$cmd" in
    start)
        log "Activating firewall script generated TIMESTAMP"
        check_nft
        prolog_commands
        configure_interfaces
        stop_action
        script_body || run_epilog_and_exit 1
        ip_forward
        epilog_commands
        log "Firewall script is done"
        RETVAL=$?
        ;;

    stop)
        stop_action
        RETVAL=$?
        ;;

    status)
        status_action
        RETVAL=$?
        ;;

    block)
        block_action
        RETVAL=$?
        ;;

    reload)
        $0 stop
        $0 start
        RETVAL=$?
        ;;

    interfaces)
        configure_interfaces
        RETVAL=$?
        ;;

    test_interfaces)
        FWBDEBUG="echo"
        configure_interfaces
        RETVAL=$?
        ;;

    *)
        echo "Usage $0 [start|stop|status|block|reload|interfaces|test_interfaces]"
        ;;

esac

exit "$RETVAL"
