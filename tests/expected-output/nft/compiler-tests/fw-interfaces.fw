#!/usr/sbin/nft -f
#
#  This is automatically generated file. DO NOT MODIFY !
#
#  Firewall Builder  fwf VERSION
#
#  Generated TIMESTAMP
#

flush ruleset

table ip filter {
    chain input {
        type filter hook input priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 0 (eth1)
        #
        iifname "eth1" tcp dport 22 ct state new accept
        #
        # Rule 4 (eth0)
        #
        iifname "eth0" drop
        #
        # Rule 5 (global)
        #
        log prefix "RULE 5 -- DENY " level info drop
    }

    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 1 (eth0)
        #
        oifname "eth0" ip saddr 192.168.1.0/24 tcp dport 80 ct state new accept
        #
        # Rule 5 (global)
        #
        log prefix "RULE 5 -- DENY " level info drop
    }

    chain output {
        type filter hook output priority filter; policy drop;
        ct state established,related accept
        #
        # Rule 1 (eth0)
        #
        oifname "eth0" ip saddr 192.168.1.0/24 tcp dport 80 ct state new accept
        #
        # Rule 2 (global)
        #
        udp dport 53 ct state new accept
        #
        # Rule 3 (global)
        #
        ip daddr 203.0.113.42 tcp dport 80 ct state new accept
        #
        # Rule 5 (global)
        #
        log prefix "RULE 5 -- DENY " level info drop
    }
}
